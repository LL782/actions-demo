name: Test workflow
run-name: Test workflow ${{ github.run_number }}

defaults:
  run:
    shell: bash

on:
  push:
    branches: [main]
    paths-ignore: [.github/**]
  pull_request:
    branches: [main]
  schedule:
    - cron: "15 6 * * 0"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Hello'
